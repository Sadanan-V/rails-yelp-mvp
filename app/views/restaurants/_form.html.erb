<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant::CATEGORIES.map { |category| category.capitalize }, prompt: "Select a category" %>
  <%= f.submit "Submit", class:"btn btn-success" %>
<% end %>
